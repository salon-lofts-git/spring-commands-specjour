RSpec.describe Spring::Commands::Specjour do
  it "has a version number" do
    expect(Spring::Commands::Specjour::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
